struct Point {
    int x,
    int y
}

func (Point p) magnitudeSquared() -> int {
    return p.x * p.x + p.y * p.y
}

int total := 0
for i in 0..150000 {
    Point p := Point{x: i, y: i + 1}
    total := total + p.magnitudeSquared()
}
print total
