# Integration tests

Each case here is a complete `.butter` program run end to end through the
same loader → compiler → VM pipeline the CLI uses, with everything it
prints checked against a saved expected output — a black-box test of the
language, as opposed to the unit tests embedded in each `src/*.zig` file,
which test one stage (lexer, parser, compiler, VM) in isolation.

| File | Problem |
|---|---|
| [fizzbuzz.butter](cases/fizzbuzz.butter) | FizzBuzz, 1 through 20. |
| [factorial.butter](cases/factorial.butter) | `n!` for `n` in `0..10`, iteratively. |
| [fibonacci.butter](cases/fibonacci.butter) | `fib(n)` for `n` in `0..9`, recursively. |
| [bubble_sort.butter](cases/bubble_sort.butter) | Sorts a fixed-size array ascending, in place. |
| [sieve_of_eratosthenes.butter](cases/sieve_of_eratosthenes.butter) | Primes up to 30, via a `bool[]` sieve. |
| [gcd_lcm.butter](cases/gcd_lcm.butter) | Greatest common divisor and least common multiple. |
| [binary_search.butter](cases/binary_search.butter) | Binary search over a sorted array, including not-found. |
| [collatz.butter](cases/collatz.butter) | Collatz step counts for 1 through 20. |
| [array_stats.butter](cases/array_stats.butter) | Sum, float average, min, and max of an array. |
| [quadratic_formula.butter](cases/quadratic_formula.butter) | Quadratic roots via the bundled `math.std.butter`'s `sqrt`/`pow`. |
| [string_indexing.butter](cases/string_indexing.butter) | Hand-rolled `key=value;...` parsing via `s[i]` (single-character compare) and `s[a..b]` (slicing), plus basic index/slice reads. |
| [strings.butter](cases/strings.butter) | String concatenation (`+`) and lexicographic ordering (`<`/`<=`/`>`/`>=`), including a bubble sort by string value. |
| [args.butter](cases/args.butter) | The bare `args` keyword — run with `expectCaseOutputWithArgs`, which passes a fixed argv instead of the default empty one. |
| [exit_code.butter](cases/exit_code.butter) | A grep-style tool built on `args` + `exit`: exits 0 with a match printed, 1 with nothing found, or 2 with a usage line and no argument at all. Has no single `.expected` (three different argv scenarios, three different outputs) — run directly with `runCapture`, which returns both the printed output and the requested exit code. |

Each `<name>.butter` has a matching `<name>.expected` holding exactly what
it should print, byte for byte (generated by actually running the
program, not transcribed by hand). [integration_test.zig](integration_test.zig)
embeds both at compile time via `@embedFile`, runs the program in memory
(no subprocess, no real stdout), and diffs the captured output against the
`.expected` file with `std.testing.expectEqualStrings`. It also has a
couple of error-path cases (division by zero, an out-of-bounds index) that
assert on the `RuntimeError` returned instead, since there's no printed
output to compare for a program that's expected to fail.

Run just these:

```bash
zig build test-integration
```

Run everything (unit tests + these):

```bash
zig build test
```

To add a case: drop `<name>.butter` in `cases/`, run it once to capture
its real output into `cases/<name>.expected`, and add one `test { ... }`
block to `integration_test.zig` calling `expectCaseOutput("<name>")`.

# Performance tests

[performance_test.zig](performance_test.zig) times, in milliseconds, how
long the same loader → compiler → VM pipeline takes on programs under
`perf_cases/`, each leaning on one "common action" a real Butter program
does a lot of, rather than checking printed output:

| File | Common action |
|---|---|
| [loop_sum.butter](perf_cases/loop_sum.butter) | A tight 4,000,000-iteration arithmetic loop. |
| [fib_recursive.butter](perf_cases/fib_recursive.butter) | Recursive calls — `fib(27)` the naive way (~832k calls). |
| [bubble_sort.butter](perf_cases/bubble_sort.butter) | Array indexing/mutation — an O(n²) sort of a 600-element array. |
| [list_push.butter](perf_cases/list_push.butter) | Heap list growth — `push`-ing 80,000 elements, then reading them all back. |
| [map_ops.butter](perf_cases/map_ops.butter) | Map hashing — 8,000 inserts followed by 8,000 `has()` lookups. |
| [string_concat.butter](perf_cases/string_concat.butter) | Heap string allocation — 3,000 rounds of `+` concatenation. |
| [function_calls.butter](perf_cases/function_calls.butter) | Call/return overhead — 2,000,000 calls to a trivial non-recursive function. |

Each test reports `compile` (lex + parse + import resolution + codegen)
and `run` (executing the compiled bytecode) separately, since only the
latter is what VM optimization work actually moves. Both are printed, not
just asserted on, so the numbers are visible without a debugger; each
test only asserts a generous upper bound (a regression guard against
something like an accidental O(n) → O(n²) change, not a performance
target — see the threshold argument to `expectCasePerformance` in
performance_test.zig).

Run just these:

```bash
zig build test-performance
```

These do not run as part of `zig build test` — they're slow by design (a
few of the workloads above are chosen to take tens to hundreds of
milliseconds) and would make the default suite noticeably slower without
adding correctness coverage.

Debug builds (the default) are much slower than release ones and not
representative of real-world performance; for numbers worth comparing
against a previous optimization pass, build in release mode:

```bash
zig build test-performance -Doptimize=ReleaseFast
```

Sample `ReleaseFast` output (one run, this machine — expect noise of a few
percent run to run, and a very different machine to differ by much more;
treat this as a baseline snapshot to compare future runs against, not a
guarantee):

```
loop_sum         compile=    0.075ms  run=  235.739ms  total=  235.813ms
fib_recursive    compile=    0.042ms  run=   26.638ms  total=   26.679ms
bubble_sort      compile=    0.058ms  run=   28.298ms  total=   28.356ms
list_push        compile=    0.041ms  run=   12.035ms  total=   12.076ms
map_ops          compile=    0.041ms  run=  206.131ms  total=  206.172ms
string_concat    compile=    0.033ms  run=   10.586ms  total=   10.620ms
function_calls   compile=    0.036ms  run=  149.319ms  total=  149.354ms
```

`compile` is negligible everywhere, as expected — it's `run` that matters
for VM optimization work. `map_ops` (~13µs/insert-or-lookup) and
`function_calls` (~75ns/call) are the priciest per-operation costs here
and the first places worth profiling.

To add a case: drop `<name>.butter` in `perf_cases/` (sized so it takes
somewhere from a few to a few hundred milliseconds — long enough that
timer resolution and OS scheduling noise don't dominate, short enough
that the suite stays usable), and add one `test { ... }` block to
performance_test.zig calling
`expectCasePerformance("<name>", <threshold-in-ms>)`.
