func collatzSteps(int n) -> int {
    int steps := 0
    while n != 1 {
        if n % 2 == 0 {
            n := n / 2
        } else {
            n := 3 * n + 1
        }
        steps := steps + 1
    }
    return steps
}

for i in 1..21 {
    print collatzSteps(i)
}
